# MEMORA

## Run (client + backend)

From the repo root (after `backend` venv setup and `client` deps):

```bash
npm install
npm run dev
```

- Client: http://localhost:3000
- API: http://localhost:8000
